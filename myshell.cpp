#include <iostream>
#include <string>
#include "parse.hpp"
int main(int argc, char** args) {
    bool cont = true;
    do {
        std::cout << "$$$ ";
        std::string userInput; // treat with gloves
        std::cin >> userInput;
        
    } while(cont);
}